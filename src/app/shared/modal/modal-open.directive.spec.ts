import { ModalOpenDirective } from './modal-open.directive';

describe('ModalOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalOpenDirective();
    expect(directive).toBeTruthy();
  });
});
